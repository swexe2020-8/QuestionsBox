<!doctype html>
<html>
<head>
   <meta charset="utf-8"/>
   <title>質問アプリQbox</title>
    <link rel="stylesheet" href="css/reverseLNavigation.css">
</head>

<body>
    <header id="pageBody">
      <h1>質問解決Qbox</h1>
    </header>
    
    
    <main>
        <div id="pageSub">
            <h2>質問一覧</h2>
            <ul>
              <% @qtitles.each do |qtitle| %>
                <li>
                    <%= link_to qtitle.title, qtitles_show_path(qtitle.id) %>
                    <%= link_to '---[削除]', qtitle_delete_path(qtitle.id), method: :delete, data: {confirm: "削除してもよろしいですか？"} %>
                </li>
              <% end %>
            </ul>
        </div>
        
        
        <div class="content">
            <div class="box7">
                <h2>質問してみよう！</h2>
　　　　　　</div>
            <h2>質問を投稿</h2>
            <%= form_for @newQtitle, :url => {:action => 'create'} do |f| %>
              <%= f.text_field :title %>
              <%= f.submit %>
            <% end %>
        </div>
    </main>
</body>




